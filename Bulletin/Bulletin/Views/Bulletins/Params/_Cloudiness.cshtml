@using Bulletin.Repositories;
@using Bulletin.Models;
@using Bulletin.Common;


            <div id="cloudiness-panel" class="panel panel-primary">
                <div id="cloudiness-panel-heading" class="panel-heading lg">Ночь</div>
                <div class="panel-body">
                <table class="table">
                    <tbody>
                        <tr class="something">
                            
                            <td class="col-md-12">
                                <select id="bulletin-@(ViewBag.forecast)-cloudiness-night" class="selectpicker" data-style="btn-primary" data-live-search="true">
                                    @foreach (var cloudiness in ((IRepository<Cloudiness>)(new CloudinessRepository())).GetAll())
                                    {
                                        <option value="@cloudiness.ID">@cloudiness.Name</option>
                                    }                                                        
                                </select>
                            </td>
                            
                        </tr>
                    </tbody>
                </table>                   
            </div>
        </div>

        